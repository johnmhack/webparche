# Netlify — sitio estático Parche + Torker
# Backend Django: local (127.0.0.1:8000) o proxy /api cuando despliegues el servidor
[build]
  publish = "."

# Cuando tengas backend en producción, descomenta y apunta a tu servidor:
# [[redirects]]
#   from = "/api/*"
#   to = "https://TU-SERVIDOR/api/:splat"
#   status = 200
